server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        git:
          uri:
#- localhost:8888/application-qa/default